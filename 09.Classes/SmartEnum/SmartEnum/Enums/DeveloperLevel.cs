namespace SmartEnum.Enums;

/***
 * Enumeration types or simply Enums are value types that we can use to represent a group of named constants.
 * By default, the underlying type of Enums in .NET is int whose value starts at
 * zero and is incremented by one for each additional constant. 
 */

public enum DeveloperLevel
{
	Junior,
	Regular,
	Senior
}